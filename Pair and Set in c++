#include <iostream>
#include <vector>
#include <set>
using namespace std;

/* ==========================
   1. Student Record Using Pair
   ==========================

int main() {
    pair<int, string> student;

    student.first = 101;
    student.second = "Simran";

    cout << "Roll No: " << student.first << endl;
    cout << "Name: " << student.second << endl;

    return 0;
}
*/

/* ==========================
   2. Unique Elements
   ==========================

int main() {
    vector<int> arr = {1, 2, 2, 3, 4, 4, 5};

    set<int> s(arr.begin(), arr.end());

    cout << "Unique Elements: ";
    for(auto x : s)
        cout << x << " ";

    return 0;
}
*/

/* ==========================
   3. Count Distinct Elements
   ==========================

int main() {
    int arr[] = {1, 2, 2, 3, 4, 4, 5};
    int n = sizeof(arr) / sizeof(arr[0]);

    set<int> s;

    for(int i = 0; i < n; i++)
        s.insert(arr[i]);

    cout << "Count of Distinct Elements = " << s.size();

    return 0;
}
*/

/* ==========================
   4. Union of Arrays
   ==========================

int main() {
    int a[] = {1, 2, 3, 4};
    int b[] = {3, 4, 5, 6};

    set<int> s;

    for(int i = 0; i < 4; i++)
        s.insert(a[i]);

    for(int i = 0; i < 4; i++)
        s.insert(b[i]);

    cout << "Union: ";
    for(auto x : s)
        cout << x << " ";

    return 0;
}
*/

/* ==========================
   5. Intersection of Arrays
   ==========================

int main() {
    int a[] = {1, 2, 3, 4};
    int b[] = {3, 4, 5, 6};

    set<int> s(a, a + 4);

    cout << "Intersection: ";

    for(int i = 0; i < 4; i++) {
        if(s.find(b[i]) != s.end())
            cout << b[i] << " ";
    }

    return 0;
}
*/

/* ==========================
   6. Remove Duplicates
   ==========================

int main() {
    vector<int> arr = {1, 2, 2, 3, 4, 4, 5};

    set<int> s(arr.begin(), arr.end());

    cout << "Array after removing duplicates: ";

    for(auto x : s)
        cout << x << " ";

    return 0;
}
*/
