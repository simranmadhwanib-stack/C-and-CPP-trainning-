/* ==========================
   1. Print Address
   ==========================

#include <stdio.h>

int main()
{
    int num = 10;
    printf("Value = %d\n", num);
    printf("Address = %p", &num);
    return 0;
}

*/

/* ==========================
   2. Swap Using Pointers
   ==========================

#include <stdio.h>

void swap(int *a, int *b)
{
    int temp;
    temp = *a;
    *a = *b;
    *b = temp;
}

int main()
{
    int x, y;

    printf("Enter two numbers: ");
    scanf("%d%d", &x, &y);

    swap(&x, &y);

    printf("After Swapping: %d %d", x, y);

    return 0;
}

*/

/* ==========================
   3. Array Sum Using Pointers
   ==========================

#include <stdio.h>

int main()
{
    int arr[5], i, sum = 0;
    int *ptr = arr;

    printf("Enter 5 elements:\n");

    for(i = 0; i < 5; i++)
        scanf("%d", &arr[i]);

    for(i = 0; i < 5; i++)
        sum += *(ptr + i);

    printf("Sum = %d", sum);

    return 0;
}

*/

/* ==========================
   4. Largest Element Using Pointers
   ==========================

#include <stdio.h>

int main()
{
    int arr[5], i;
    int *ptr = arr;

    printf("Enter 5 elements:\n");

    for(i = 0; i < 5; i++)
        scanf("%d", &arr[i]);

    int largest = *ptr;

    for(i = 1; i < 5; i++)
    {
        if(*(ptr + i) > largest)
            largest = *(ptr + i);
    }

    printf("Largest Element = %d", largest);

    return 0;
}

*/

/* ==========================
   5. Traverse Array Using Pointers
   ==========================

#include <stdio.h>

int main()
{
    int arr[5], i;
    int *ptr = arr;

    printf("Enter 5 elements:\n");

    for(i = 0; i < 5; i++)
        scanf("%d", &arr[i]);

    printf("Array Elements:\n");

    for(i = 0; i < 5; i++)
        printf("%d ", *(ptr + i));

    return 0;
}

*/

/* ==========================
   6. Reverse Array Using Pointers
   ==========================

#include <stdio.h>

int main()
{
    int arr[5], i;
    int *ptr = arr;

    printf("Enter 5 elements:\n");

    for(i = 0; i < 5; i++)
        scanf("%d", &arr[i]);

    printf("Reverse Array:\n");

    for(i = 4; i >= 0; i--)
        printf("%d ", *(ptr + i));

    return 0;
}

*/
